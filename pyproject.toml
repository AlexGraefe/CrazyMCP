[project]
name = "crazymcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cflib2",
    "httpx>=0.28.1",
    "langchain[openrouter]>=1.3.9",
    "mcp[cli]>=1.27.2",
]

[tool.uv.sources]
cflib2 = { git = "https://github.com/bitcraze/crazyflie-lib-python-v2.git", rev = "main" }
